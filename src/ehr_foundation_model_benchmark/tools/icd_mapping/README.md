# Run ICD Mapping procedure

Converts ICD codes in input MEDs data.  

## Instructions

Set `INPUT_MEDS` and `OUTPUT_MEDS_TEMP` paths in run.sh. Then `./run.sh` will generated the converted MEDS data in `OUTPUT_MEDS_TEMP`.
